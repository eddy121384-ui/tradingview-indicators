# Issue #68 — Final Release-Candidate Static Finding

Date: 2026-09-08

Decision: **PASS TO FINAL TRADINGVIEW SMOKE TEST — NOT FINAL PRODUCTION AUTHORIZATION**

## Generated release candidate

`indicators/wyckoff-regime-radar/src/chase-risk-market-regime-radar-issue68-rc.pine`

The RC was generated by GitHub Actions from the frozen `v0.5.2.1` lineage through the accepted Issue #66 C-2 transforms, Issue #68 exact symmetric HARD repair, yield-safe representation split, and approved presentation defaults.

Bot generation commit:

`e50dd72a37f28c3cc575c04cf2b76fc423627ebc`

No new semantic version is assigned at this stage. Issue #66 accepted C-2 as a downstream baseline but did not authorize a `v0.6` or `v0.5.3` production name. The source is therefore explicitly labeled `Issue #68 RC` until Eddy authorizes production and version naming.

## Static contract — PASS

The generator round-trips the RC identification title/header back to the validated yield-safe HARD production candidate and requires byte-for-byte identity outside those identification lines.

Verified required content includes:

- `Auto / Price Log / Yield Level` representation input;
- static Auto yield detection using `syminfo.type == "bond" and syminfo.currency == "NONE"`;
- exact S1 current Bear gate and bounded DownEx gate;
- exact S4 mirrored current Bull gate and bounded UpEx gate;
- S1/S4 direct gate routing through the bounded gates;
- production Volume / MTF / Divergence inputs and lower-timeframe request path;
- full visuals, dashboard and alerts;
- downside panic-risk default dashed;
- approved six-phase palette defaults.

Forbidden-content guard confirms the clean RC contains no:

- A/B regression harness (`issue68AB`);
- A/B diagnostic plots;
- Strategy Tester / strategy entry or close code;
- rejected Stateful current-context logic.

Manual connector inspection after generation also confirmed the critical representation, HARD routing, presentation defaults, and absence of `issue68AB` leakage.

## Relationship to six-market regression

The frozen A/B regression passed on FR10Y, DE10Y, US10Y, GB10Y, AU10Y and JP10Y with `Invariant fails = 0` on every market. The detailed finding is recorded in:

`research/decisions/issue-68-yield-safe-hard-six-market-regression-final-finding.md`

The RC contains the B/HARD production logic from that accepted comparison, without the A/B audit harness.

## Remaining gate

Only a final TradingView smoke test remains before PR #73 may move from Draft to Ready for Review.

Required smoke checks:

1. Pine compiles and adds to chart.
2. No runtime or 64-plot-limit error occurs.
3. Full production dashboard/visuals are present; no A/B audit table appears.
4. On a government 10Y yield chart with representation `Auto`, the indicator resolves to the yield-safe path and historical output remains continuous.
5. Downside panic-risk line is dashed by default.
6. Six phase background defaults are visually correct: Acc light green, Markup deep green, Reacc light blue, Distribution light yellow, Markdown red, Redist orange.

Do not change thresholds, weights, confirmation behavior, representation routing, or HARD semantics during this smoke pass.

Issue #68 remains open and PR #73 remains Draft/Open until the smoke test passes and Eddy explicitly authorizes the production/review transition.