name: Wyckoff Issue 68 DownEx Gate Channels

on:
  push:
    branches:
      - research/issue-68-lifecycle-retest-symmetry-repaired
    paths:
      - "indicators/wyckoff-regime-radar/research/generate_issue68_downex_gate_channel_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/decisions/issue-68-downex-gate-channel-preregistration.md"
      - ".github/workflows/wyckoff-issue68-downex-gate-channels.yml"
  pull_request:
    branches: [main]
    paths:
      - "indicators/wyckoff-regime-radar/research/generate_issue68_downex_gate_channel_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/decisions/issue-68-downex-gate-channel-preregistration.md"
      - ".github/workflows/wyckoff-issue68-downex-gate-channels.yml"

permissions:
  contents: write

jobs:
  downex-gate-channels:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: indicators/wyckoff-regime-radar/research
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref_name }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile generator
        run: python -m py_compile generate_issue68_downex_gate_channel_audit_pine.py
      - name: Generate TradingView DownEx gate-channel audit
        run: |
          python generate_issue68_downex_gate_channel_audit_pine.py \
            --output generated/wyckoff-issue68-downex-gate-channel-audit.pine
          test -s generated/wyckoff-issue68-downex-gate-channel-audit.pine
          grep -q 'DownEx Gate-Channel Decomposition' generated/wyckoff-issue68-downex-gate-channel-audit.pine
          grep -q 'DIRECT' generated/wyckoff-issue68-downex-gate-channel-audit.pine
          grep -q 'INDIRECT' generated/wyckoff-issue68-downex-gate-channel-audit.pine
          grep -q 'DownEx > Support' generated/wyckoff-issue68-downex-gate-channel-audit.pine
          grep -q 'S1 RAW FROZEN' generated/wyckoff-issue68-downex-gate-channel-audit.pine
          ! grep -q 'strategy\.' generated/wyckoff-issue68-downex-gate-channel-audit.pine
          echo 'DownEx gate-channel audit contract PASS'
      - name: Commit generated artifact on branch push
        if: github.event_name == 'push' && github.ref == 'refs/heads/research/issue-68-lifecycle-retest-symmetry-repaired'
        working-directory: .
        env:
          TARGET_BRANCH: research/issue-68-lifecycle-retest-symmetry-repaired
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add indicators/wyckoff-regime-radar/research/generated/wyckoff-issue68-downex-gate-channel-audit.pine
          if git diff --cached --quiet; then
            echo "DownEx gate-channel artifact already current."
            exit 0
          fi
          git commit -m "research: generate Issue 68 DownEx gate-channel audit [skip ci]"
          for attempt in 1 2 3; do
            git fetch origin "${TARGET_BRANCH}"
            git rebase "origin/${TARGET_BRANCH}"
            if git push origin "HEAD:${TARGET_BRANCH}"; then
              exit 0
            fi
            echo "push race on attempt ${attempt}; retrying"
            sleep $((attempt * 2))
          done
          exit 1
