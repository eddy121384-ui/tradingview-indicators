name: Wyckoff Issue 68 TOP Bull Strong Gate

on:
  push:
    branches:
      - research/issue-68-lifecycle-retest-symmetry-repaired
    paths:
      - "indicators/wyckoff-regime-radar/research/generate_issue68_cross_market_top_bull_strong_gate_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/decisions/issue-68-cross-market-top-bull-strong-gate-preregistration.md"
      - ".github/workflows/wyckoff-issue68-top-bull-strong-gate.yml"
  pull_request:
    branches: [main]
    paths:
      - "indicators/wyckoff-regime-radar/research/generate_issue68_cross_market_top_bull_strong_gate_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/decisions/issue-68-cross-market-top-bull-strong-gate-preregistration.md"
      - ".github/workflows/wyckoff-issue68-top-bull-strong-gate.yml"

permissions:
  contents: write

jobs:
  top-bull-strong-gate:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: indicators/wyckoff-regime-radar/research
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref_name }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile generator
        run: python -m py_compile generate_issue68_cross_market_top_bull_strong_gate_audit_pine.py
      - name: Generate TOP Bull -> Strong gate TradingView audit
        run: |
          python generate_issue68_cross_market_top_bull_strong_gate_audit_pine.py \
            --output generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          test -s generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          grep -q 'TOP Bull -> Strong Gate Attribution' generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          grep -q 'FIRST FAIL TOTALS' generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          grep -q 'Formal Bull acquire' generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          grep -q 'candidateConflict' generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          ! grep -q 'strategy\.' generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          echo 'TOP Bull strong-gate audit contract PASS'
      - name: Commit generated artifact on branch push
        if: github.event_name == 'push' && github.ref == 'refs/heads/research/issue-68-lifecycle-retest-symmetry-repaired'
        working-directory: .
        env:
          TARGET_BRANCH: research/issue-68-lifecycle-retest-symmetry-repaired
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add indicators/wyckoff-regime-radar/research/generated/wyckoff-issue68-cross-market-top-bull-strong-gate-audit.pine
          if git diff --cached --quiet; then
            echo "TOP Bull strong-gate artifact already current."
            exit 0
          fi
          git commit -m "research: generate Issue 68 TOP Bull strong-gate audit [skip ci]"
          for attempt in 1 2 3; do
            git fetch origin "${TARGET_BRANCH}"
            git rebase "origin/${TARGET_BRANCH}"
            if git push origin "HEAD:${TARGET_BRANCH}"; then
              exit 0
            fi
            echo "push race on attempt ${attempt}; retrying"
            sleep $((attempt * 2))
          done
          exit 1
