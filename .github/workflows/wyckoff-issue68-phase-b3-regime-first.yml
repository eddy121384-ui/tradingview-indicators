name: Wyckoff Issue 68 Phase B3 Regime-first

on:
  push:
    branches:
      - research/issue-68-lifecycle-retest-symmetry-repaired
    paths:
      - "indicators/wyckoff-regime-radar/research/issue68_lifecycle_v3_regime_first.py"
      - "indicators/wyckoff-regime-radar/research/diagnose_issue68_phase_b3_regime_first.py"
      - "indicators/wyckoff-regime-radar/research/test_issue68_phase_b3_regime_first.py"
      - "indicators/wyckoff-regime-radar/research/generate_issue68_phase_b3_regime_first_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/test_issue68_phase_b3_regime_first_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/decisions/issue-68-phase-b3-regime-first-v3-preregistration.md"
      - ".github/workflows/wyckoff-issue68-phase-b3-regime-first.yml"
  pull_request:
    branches: [main]
    paths:
      - "indicators/wyckoff-regime-radar/research/issue68_lifecycle_v3_regime_first.py"
      - "indicators/wyckoff-regime-radar/research/diagnose_issue68_phase_b3_regime_first.py"
      - "indicators/wyckoff-regime-radar/research/test_issue68_phase_b3_regime_first.py"
      - "indicators/wyckoff-regime-radar/research/generate_issue68_phase_b3_regime_first_audit_pine.py"
      - "indicators/wyckoff-regime-radar/research/test_issue68_phase_b3_regime_first_audit_pine.py"
      - ".github/workflows/wyckoff-issue68-phase-b3-regime-first.yml"

permissions:
  contents: write

jobs:
  regime-first-semantic-contract:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: indicators/wyckoff-regime-radar/research
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref_name }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check numpy pandas
      - name: Compile B3 tooling
        run: |
          python -m py_compile issue68_lifecycle_v3_regime_first.py
          python -m py_compile diagnose_issue68_phase_b3_regime_first.py
          python -m py_compile generate_issue68_phase_b3_regime_first_audit_pine.py
          python -m py_compile test_issue68_phase_b3_regime_first.py
          python -m py_compile test_issue68_phase_b3_regime_first_audit_pine.py
      - name: Run frozen v3 state-machine contract
        run: python -m unittest -v test_issue68_phase_b3_regime_first
      - name: Run v3 Pine transport contract
        run: python -m unittest -v test_issue68_phase_b3_regime_first_audit_pine
      - name: Run burned-data semantic diagnostic
        run: |
          python diagnose_issue68_phase_b3_regime_first.py \
            --json reports/issue-68-phase-b3-regime-first.json \
            --md reports/issue-68-phase-b3-regime-first.md
      - name: Enforce no performance metric keys
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path('reports/issue-68-phase-b3-regime-first.json').read_text())
          forbidden = ('pnl', 'return', 'sharpe', 'drawdown', 'hit_rate', 'transaction_cost', 'profit')
          bad = []
          def walk(x, path=''):
              if isinstance(x, dict):
                  for k, v in x.items():
                      lk = str(k).lower()
                      if any(t in lk for t in forbidden):
                          bad.append(path + '/' + str(k))
                      walk(v, path + '/' + str(k))
              elif isinstance(x, list):
                  for i, v in enumerate(x):
                      walk(v, path + f'/{i}')
          walk(data)
          if bad:
              raise SystemExit('forbidden performance keys: ' + ', '.join(bad))
          PY
      - name: Generate clean B3 TradingView audit Pine
        run: |
          python generate_issue68_phase_b3_regime_first_audit_pine.py \
            --output generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
          test -s generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
          grep -q 'Issue #68 Regime-first v3 Audit' generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
          ! grep -q 'strategy\.' generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
          ! grep -q 'issue68EarlyFail' generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
          ! grep -q 'issue68ArmedDir' generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
      - name: Commit B3 report and audit artifact on branch push
        if: github.event_name == 'push' && github.ref == 'refs/heads/research/issue-68-lifecycle-retest-symmetry-repaired'
        working-directory: .
        env:
          TARGET_BRANCH: research/issue-68-lifecycle-retest-symmetry-repaired
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add indicators/wyckoff-regime-radar/research/reports/issue-68-phase-b3-regime-first.json
          git add indicators/wyckoff-regime-radar/research/reports/issue-68-phase-b3-regime-first.md
          git add indicators/wyckoff-regime-radar/research/generated/wyckoff-issue68-phase-b3-regime-first-audit.pine
          if git diff --cached --quiet; then
            echo "B3 artifacts already current."
            exit 0
          fi
          git commit -m "research: record Issue 68 B3 regime-first semantic artifacts [skip ci]"
          for attempt in 1 2 3; do
            git fetch origin "${TARGET_BRANCH}"
            git rebase "origin/${TARGET_BRANCH}"
            if git push origin "HEAD:${TARGET_BRANCH}"; then
              exit 0
            fi
            echo "push race on attempt ${attempt}; retrying"
            sleep $((attempt * 2))
          done
          exit 1
